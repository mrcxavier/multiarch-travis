language: bash

os: linux

dist: bionic

services: 
  - docker 

jobs: 
  include: 
   - stage: Building x86_64 Image 
     arch: amd64
     script: chmod +x ./build.sh; ./build.sh  
  include: 
   - stage: Building ppc64le Image 
     arch: ppc64le
     script: chmod +x ./build.sh; ./build.sh  

