language: bash

os: linux

dist: bionic

services: 
  - docker 

jobs: 
  include: 
    - stage: x86_64 Image 
      arch: amd64
      script: chmod +x ./build.sh; ./build.sh  
  include: 
    - stage: ppc64le Image 
      arch: ppc64le
      script: chmod +x ./build.sh; ./build.sh  
