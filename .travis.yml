language: bash

os: linux

services: 
  - docker 

dist: bionic

jobs: 
  include: 
   - stage: ppc64le
     arch: Building ppc64le
     script: chmod +x ./build.sh; ./build.sh

  include: 
   - stage: Building x86_64 Image 
     arch: amd64
     script: chmod +x ./build.sh; ./build.sh  

